cmake_minimum_required(VERSION 2.8)
project( sudoku )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( sudoku main.cpp solver.cpp sudokuBoard.cpp imgToNum.cpp )
target_link_libraries( sudoku ${OpenCV_LIBS} tesseract )
